import { DisplayColourPipe } from './display-colour.pipe';

describe('DisplayColourPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayColourPipe();
    expect(pipe).toBeTruthy();
  });
});
